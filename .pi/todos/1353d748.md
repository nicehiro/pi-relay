{
  "id": "1353d748",
  "title": "pi-relay Phase 5: Thread-per-session",
  "tags": [
    "pi-relay",
    "phase-5"
  ],
  "status": "closed",
  "created_at": "2026-03-11T05:42:22.970Z"
}

## Scope
Use Discord threads to isolate pi sessions within a channel.

## Tasks
1. Create Discord thread when user starts a new conversation
2. Map thread ID → pi session (via `pi.sendUserMessage` with context)
3. `/relay thread` command to explicitly create a new session thread
4. Auto-name thread from first message (truncated)
5. Persist thread↔session mapping (survive extension restart)

## Depends on
- Phase 1 complete
- Consider session management implications: each thread = fresh context? or same pi session?
