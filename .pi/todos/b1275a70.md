{
  "id": "b1275a70",
  "title": "pi-relay Phase 2: Push notifications (discord_send tool)",
  "tags": [
    "pi-relay",
    "phase-2"
  ],
  "status": "closed",
  "created_at": "2026-03-11T05:42:22.959Z"
}

## Scope
Register a `discord_send` tool so pi (LLM or scripts via control socket) can push messages to Discord.

## Tasks
1. Register `discord_send` tool with params: `message` (required), `channel` (optional, defaults to notify channel)
2. Add `notify.channel` to config
3. Handle case where Discord is not connected (queue or error)
4. Test via pi control socket / send_to_session

## Depends on
- Phase 1 complete
